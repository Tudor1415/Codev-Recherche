package io.gitlab.chaver.minimax.kappalab.algorithm;

import io.gitlab.chaver.minimax.kappalab.io.KappalabInput;
import io.gitlab.chaver.minimax.kappalab.io.KappalabMethod;
import io.gitlab.chaver.minimax.kappalab.io.KappalabOutput;
import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class GLSCplexTest {

    @Test
    void call() throws Exception {
        /*double[][] alternatives = {
                {0.8, 0.9, 0.7},
                {0.9, 0.8, 0.7},
                {0.5, 1.0, 0.6}
        };
        Number[][] preferences = {
                {1, 2, 0.1},
                {2, 3, 0.1}
        };
        KappalabInput input = new KappalabInput(Arrays.asList(alternatives), Arrays.asList(preferences), 2, KappalabMethod.gls);
        GLSCplex gls = new GLSCplex(input);
        KappalabOutput output = gls.call();
        System.out.println(output);*/
    }
}